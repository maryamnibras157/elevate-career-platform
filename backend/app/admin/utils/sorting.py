from typing import Any

def apply_sorting(query: Any, sort_by: str, sort_order: str = "asc") -> Any:
    """
    Generic sorting helper to apply dynamic sorting to a query.
    This is a placeholder that will be expanded based on the ORM used.
    """
    return query
